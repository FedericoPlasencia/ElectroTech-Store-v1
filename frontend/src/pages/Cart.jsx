import React from 'react'
import { useCart } from '../context/CartContext.jsx'
import { Link } from 'react-router-dom'
export default function Cart(){ const { cart, removeFromCart, clearCart, total } = useCart(); if(cart.length===0) return <div className='p-8 text-center'>Tu carrito está vacío</div>; return (<div className='p-8 max-w-4xl mx-auto'><h2 className='text-2xl font-bold mb-4'>Tu carrito</h2><div className='space-y-4'>{cart.map(item=> (<div key={item.id} className='flex items-center justify-between border-b pb-2'><div className='flex items-center gap-4'><img src={item.image} alt={item.name} className='w-16 h-16 object-cover rounded' /><div><h3 className='font-semibold'>{item.name}</h3><p>${item.price} x {item.qty}</p></div></div><button onClick={()=>removeFromCart(item.id)} className='text-red-600'>Quitar</button></div>))}</div><div className='mt-6 flex justify-between items-center'><button onClick={clearCart} className='bg-gray-200 px-4 py-2 rounded'>Vaciar</button><div><div className='text-xl font-bold'>Total: ${total}</div><Link to='/checkout' className='mt-2 inline-block bg-green-500 text-white px-4 py-2 rounded'>Finalizar compra</Link></div></div></div>) }
